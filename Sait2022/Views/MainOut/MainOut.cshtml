@model IEnumerable<Sait2022.Domain.Model.MainOut>

@{
    ViewData["Title"] = "Ответы";
}

<title>Вариант 1</title>

<table class="table">
    @foreach (var item in Model)
{
    <tr>
        <td>
            @item.ValueAnswer
        </td>
        <td>
            @item.CheckAnswer
        </td>
    </tr>
    }
</table>