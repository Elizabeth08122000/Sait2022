@using Microsoft.AspNetCore.Identity
@using Sait2022.Domain.Model

@inject SignInManager<Users> SignInManager
@inject UserManager<Users> UserManager

@if (SignInManager.IsSignedIn(User))
{
    <ul class="navbar-nav">
        <li class="nav-item dropdown">
            <a class="nav-link" href="" id="navbarDropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                <p>
                    Пользователь: @UserManager.GetUserName(User)
                </p>
                <div class="ripple-container"></div>
            </a>
            <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdownMenuLink">
                <a class="dropdown-item" asp-controller="Account" asp-action="Logout">Выйти</a>
            </div>
        </li>
    </ul>
}
else
{
    <ul class="navbar-nav">
        <li class="nav-item dropdown">
            <a class="nav-link" href="" id="navbarDropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                <p>
                    Аккаунт
                </p>
                <div class="ripple-container"></div>
            </a>
            <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdownMenuLink">
                <a class="dropdown-item" asp-controller="Account" asp-action="RegistrationNewUser">Регистрация</a>
                <a class="dropdown-item" asp-controller="Account" asp-action="Login">Вход</a>
            </div>
        </li>
    </ul>
}
