@using Sait2022.Models
@model ICollection<QuestionsViewModel>

@{
    ViewData["Title"] = "Тест";
}


<title>Тест</title>

<body>

    <div class="container text-center">

        <div class="text-center">

            @if (Model != null)
            {
                @foreach (var item in Model)
                {
                    <p>@item.Rangs</p>
                    <p> @item.ValueQuest </p>
                    <p></p>
                }
            }
        </div>

    </div>

</body>