@model Sait2022.Domain.Model.Questions

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Answers</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
              @Html.DisplayFor(modelItem => modelItem.ValueQuest)
            </div>
            <div class="form-group">
                <label asp-for="StudentAnswer" class="control-label"></label>
                <input asp-for="StudentAnswer" class="form-control" />
                <span asp-validation-for="StudentAnswer" class="text-danger"></span>
            </div>
            <input type="hidden" asp-for="Id" />
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Questions">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
